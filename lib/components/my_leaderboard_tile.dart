import 'package:flutter/material.dart';

class MyLeaderboardTile extends StatelessWidget {
  const MyLeaderboardTile({super.key});

  @override
  Widget build(BuildContext context) {
    return Row(
      children: [],
    );
  }
}
